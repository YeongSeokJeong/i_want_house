# /large-task-orchestrator resume Checklist (Legacy Alias)

`resume` is now a legacy alias of `/large-task-orchestrator next` in Open Mode.
Use `next` for all new usage.

## Behavior

- Execute `next` Open Mode behavior exactly.
- Refer to `./next.md` checklist (Open Mode section) as the source of truth.
